%h1 Группы

= link_to 'Добавить группу', new_group_path, class: "btn btn-primary"

%table.table-hover.table-condensed
  %thead
    %tr
      %th.tbl-width200.text-center Номер
      %th
      %th
      %th

  %tbody
    - @groups.each do |group|
      %tr
        %td.tbl-width200.text-center= group.numberg
        %td= link_to 'Показать', group, class: "btn btn-primary"
        %td= link_to 'Редактировать', edit_group_path(group), class: "btn btn-warning"
        %td= link_to 'Удалить', group, method: :delete, data: { confirm: 'Вы уверены?' }, class: "btn btn-danger"

%br

